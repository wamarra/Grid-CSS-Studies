import * as React from "react";
import { Container, Header, Main } from "./main-content-styled";

const MainContent: React.FC = () => {
    return <Container>
        <Header />
        <Main />
    </Container>
}

export default MainContent;